import styles from "./examplePage.module.scss";
const page = () => {
  return (
    <div className={styles.mainWrapper}>
      <p>Berief detail something about hooks will be here</p>
    </div>
  );
};

export default page;
