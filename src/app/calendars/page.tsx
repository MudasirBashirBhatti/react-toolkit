import styles from "./calendarsPage.module.css";

const page = () => {
  return (
    <div className={styles.mainWrapper}>
      General documentation for the calendars will be here
    </div>
  );
};

export default page;
